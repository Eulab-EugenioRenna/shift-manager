import { FixedTooltipDirective } from './fixed-tooltip.directive';

describe('FixedTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new FixedTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
